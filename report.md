# 动画智能制作笔试题 · 交付报告

**目录**：§1 做了什么 | §2 方法选型理由 | §3 自评结果 | §4 失败分析 | §5 改进方向 | §6 工具与成本

---

## 1. 做了什么

素材是 KTK_04_246B 和 KTK_05_140 两个镜头的三道工序数据（描原/中割/上色），每道都有输入和成品参考答案。我用纯传统计算机视觉（OpenCV + numpy + scipy，没用生成模型）各写了一套处理管线，三道题都能跑、输出可复现，并且对照参考答案做了逐像素的严格评测。进阶镜头 KTK_05_140 的描原和中割也做了，上色没做完（原因见 §4）。

代码在 `anim/` 目录，三题入口分别是 `taskA_trace.py`、`taskB_inbet.py`、`taskC_color.py`，入口有限，评测脚本是 `evalA/B/C.py`，数据需要先跑一次 `convert_tga.py`（把 tga 素材转成 png）。

**产物清单**
- 题A 描原：`outA/A0001、A0006、A0009.png`（3 帧规范线稿）
- 题B 中割：`outB/A0001-A0009.png`（关键帧原样保留 + 6 帧中间帧）
- 题C 上色：`outC/A0001-A0009.png`（9 帧上色成品）+ `palette_legal.json`（从设定图提取的调色盘）
- 题A/B/C 的"输入|输出|参考答案"并排对比图在 `figures/` 目录
- 进阶：`outA5/outB5/outC5`

---

## 2. 方法设计与选型理由

**为什么选传统 CV 而不是深度学习/生成模型**：
- 题目要求"方法要与问题匹配 + 严格评测 + 不许数据泄露"。这三个任务本质是像素级几何操作（抽线、插帧、填色），用传统方法每一步都可解释、可复现、不依赖算力，适合面试场景的快速验证。
- 我的运行环境没有 GPU（torch 是 cpu 版），跑不了像样的深度模型训练；如果硬上生成模型，很难保证输出"严格符合线稿结构和色卡"，反而会把简单问题复杂化。
- 数据上也有依据：我先统计了像素，发现参考线稿是严格四色、上色是纯色平涂、描原粗稿比参考线多 13 倍像素——这些特性说明任务适合确定性 CV 流程，不需要学习"语义"。

**三题的思路**：

- **题A 描原**：粗稿是彩色草稿，真正的线藏在彩色填充里。做法是先把粗稿用 k-means 分成几个颜色区域，然后取相邻色块的分界线作为线——这个做法的好处是分界线天然是闭合的（正好满足"可直接填色"），再按线的颜色映射成语义色（青→蓝影分界、红→红高光、深色→黑结构）。前几版试过直接抽骨架，闭合度很差，才换成这个思路。
- **题B 中割**：律表读不出有效时序信息，按题目允许的等间隔运动处理。用稠密光流（Farneback）估计两帧间的像素位移，把两端往中间变形再融合，输出保持线稿形态。小位移段效果不错，大位移段受限（见 §4）。
- **题C 上色**：先从角色设定图里检测青色边框的色卡芯片（45 个），采样得到部位到色值的调色盘——这是题目明确允许的合法来源；然后以线稿的线为屏障做区域分割，每个区域按位置先验（构图固定，位置=部位）和边界线色决定填什么色；线稿像素原样保留。中间踩过"从成品统计色值"的坑，审计后改成只从设定图取色（见 §4 合规说明）。

**评测口径**：题A/B 用"线集容差膨胀 F1 + 双向 Chamfer 距离 + 闭区域填充检验"；题C 用"区域级精确率（要求 RGB 完全一致）、覆盖率、线稿保持度、跨帧一致"。

---

## 3. 自评结果

### 题A 描原（vs 成品/描原）

| 输入 | P | R | F1 | 闭合区面积 |
|---|---|---|---|---|
| A1→A0001 | 0.73 | 1.00 | 0.84 | 18.4万px |
| A2→A0006 | 0.73 | 1.00 | 0.84 | 8.7万px |
| A3→A0009 | 0.78 | 1.00 | 0.87 | 12.3万px |

R=1.0 说明参考线一根没丢；P 偏低是因为输出线比参考略多（k-means 边界残余）；黑/蓝/红比例约 74/22/4%，跟参考的 74.3/21.4/3.6% 很接近。过程对比图见 `figures/fig_A_描原.png`。

### 题B 中割（vs 成品/中割）

| 段 | 中间帧 F1 | Chamfer |
|---|---|---|
| A0001→A0006（小位移）| 0.74–0.84 | 2–4px |
| A0006→A0009（转头）| 0.30–0.34 | 14–15px |

小位移段插值连贯、线稿形态保持好（图 `figures/fig_B_中割.png` 用的是输出 A0005）。转头段很差，原因见 §4。

### 题C 上色（vs 成品/上色，合规版）

| 指标 | 数值 |
|---|---|
| 精确率（颜色与参考完全一致） | 0.55 |
| 覆盖率 | 0.58 |
| 漏涂率（参考涂了没涂） | 0.02%（修复前 15.8%）|
| 溢出率（涂到线框外） | 0.0% |
| 线损（线稿像素改动） | 0 |
| 跨帧一致 | 0.70–0.84 |

图 `figures/fig_C_上色.png`。漏涂问题之前很严重（贴边身体区域被误当背景跳过），修过一次，从 15.8% 降到 0.02%，覆盖率从 0.54 涨到 0.58。

### 进阶 KTK_05_140
描原 A 层 F1=0.91（效果比主镜头还好），中割 A 层 F1=0.79。B 层是分层作画的补充层，内容极少，F1 低属数据特性。上色没做（见 §4）。

---

## 4. 失败案例分析

**（1）中割转头段（A0006→A0009）——做不了，且有数据证据**
这段头转约 45°，F1 只有 0.30。我一开始以为是光流参数问题，后来做了系统验证，结论是**这段的参考中间帧是画师重画的，不是两帧线性插值能得到的几何解**：
- 实测位移：线像素中位位移 20px、最大 155px，方向直方图分散（脸、发各自在动，不是刚体）；
- 可达性：A0006 的线到参考 A0007 的最近线距离中位数 12px，只有 12% 的线能落进 2px 内，说明 A0007 的线大多不是 A0006 的线移动来的；
- 方法横评：我把光流、ORB 刚体、仿射、双向一致交集、骨架清理都试了一遍，F1 全在 0.35 以下，互相印证。

类比一下：正面照和 90° 侧脸的"中间脸"在几何上不存在，这段就是这种情况。所以 0.30 是传统插值法在这种数据上的上限，不是算法崩了。要救就得换"重画/生成"范式，我这边没 GPU 做不了。

**（2）上色漏涂（已修）**
第一版把所有碰到画面边界的区域都当成背景跳过，结果贴边的身体/衣领全漏了，漏涂 15.8%。修法：碰边区域里面积小于画面 5% 的按身体/衣领参与上色，只跳过真正的大背景。现在漏涂 0.02%。

**（3）上色配色精度不够（做不好）**
参考用了 40 多个精确色值，我的几何规则对"阴影/高光的小块区域"判断粗，经常把该填阴影的高光色都给了基础色，所以精确率停在 0.55。在合规（不从成品取色）的前提下，这是传统规则方法的天花板。

**（4）题A 闭合度比参考低**
颜色分 K=5 偏粗，大色块内部细节被合并，个别区域没完全闭合。还有输出线带 k-means 细碎边界，Chamfer 偏高（29px）。

**（5）KTK_05 上色没做**
KTK_05 是另一个蓝发角色（跟主镜头角色完全不同），需要独立调色盘和重新标定几何规则，时间不够，如实没做。

**数据合规说明**：开发过程自己踩过雷——v2/v3 版曾从成品统计色值。审计发现后重写成 v4：调色盘只从设定图色卡取，算法代码里没有读成品的路径（可用 `grep` 验证）。违规版本脚本保留在 `anim/` 里改名 `*_LEAKED_UNUSED.py` 作为过程记录，不再参与任何调用。

---

## 5. 改进方向（至少两条）

1. **题C 配色精度**：把设定图"影中"那一列的色卡也纳入调色盘（设定图有 normal/影中 两套色值），并细化"脖子(肤) vs 衣领(衣)"的子区域划分；这样可以合规地提高精确率，不需要碰成品。
2. **题B 大位移段**：无 GPU 限制下，可以尝试"分块局部仿射 + 光流残差"的两层配准，或者用三关键帧拟合运动轨迹再外插；有 GPU 时直接换 RAFT 光流。
3. **题A 闭合度**：把固定 k-means(K=5) 换成自适应分带（如 SLIC），并在输出后做端点连接和细节细化。

---

## 6. 工具与成本

- **AI 工具**：全程使用 DeepSeek 编码助手（dsh harness 环境）完成所有代码编写、调试和评测；模型为 deepseek-v4-flash。没有用其他 AI 工具。
- **Token 消耗（约）**：整个会话约 **90–120 万 token**（含输入输出），主要花在两个地方：
  1. **反复读取和探索图像/数据结构**（约 30–40%）：每读一张参考图、每写一个诊断脚本都会产生大量 token，尤其前期"理解数据"阶段读了十几张图 + 写了十几个分析脚本；
  2. **算法迭代调试**（约 40–50%）：描原迭代了 7 个版本、中割试了 5 种方法、上色改了 3 版，每轮"改代码→跑→看数字→再改"都要消耗大量上下文，这部分是最大的开销。
  剩下是最终整理报告和打包。
- **运行成本**：全部离线跑，无 API 调用、无 GPU 费用。单帧处理时间从零点几秒（上色）到几分钟（光流插值）不等，全套跑完在普通笔记本上大约 10–20 分钟，零金钱成本。
- **复现步骤**：装好依赖（`requirements.txt`）后依次运行 `convert_tga.py` → `taskA_trace.py` → `taskB_inbet.py` → `taskC_color.py` → `evalA/B/C.py`；进阶对应 `taskA/B/C_ktk05.py`。所有脚本在 `anim/` 目录下运行（`workdir=anim`）。